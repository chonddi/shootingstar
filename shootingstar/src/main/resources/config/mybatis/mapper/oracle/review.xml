<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="config.mybatis.mapper.oracle.review">

	<select id="selectReview" parameterType="int" resultType="ReviewVO">
		select * from review where smemberid = #{sMemberid}
	</select>
	
	<insert id="regitReview" parameterType="ReviewVO">
		<selectKey keyProperty="reviewNo" order="BEFORE" resultType="int">
			select review_seq.nextval from dual
		</selectKey>
			insert into review(reviewno, smemberid, content, communication, result, memberid, pno, flag)
			values(#{reviewNo}, #{sMemberid}, #{content}, #{communication}, #{result}, #{memberid}, #{pNo}, #{flag})
	</insert>

</mapper>









